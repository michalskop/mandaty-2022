name: Deploy president odds

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '15 4,16 * * *'
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install and Build 🔧
        run: |
          cd frontpage
          yarn install
          npx nuxi generate

      # - name: Deploy 🚀
      #   uses: JamesIves/github-pages-deploy-action@4.1.5
      #   with:
      #     branch: gh-pages
      #     folder: dist